// App.js (o tu archivo de entrada principal)

import React from 'react';
// Asegúrate que la ruta ahora apunte a la nueva carpeta, si es necesario
import AppNavigator from './navigation/AppNavigation';

export default function App() {
  return <AppNavigator />;
}